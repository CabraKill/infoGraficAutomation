import 'package:flutter/material.dart';

class GraphWhenPage extends StatelessWidget {
  const GraphWhenPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset("assets/when.png"),
    );
  }
}
